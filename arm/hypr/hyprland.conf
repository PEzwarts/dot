$mod = Alt

exec-once = lxsession
exec-once = pulseaudio

exec-once = waypaper --restore

exec-once = udiskie

exec-once = [workspace 1] librewolf

exec-once = [workspace 2] alacritty --command nvim -c ":ToggleTerm"

# Apple numeric keyboard layout

bind = Control_L, Return, exec, waypaper

bind = Super_L, Return, exec, alacritty --command nvim -c ":ToggleTerm"

bind = $mod, Return, exec, librewolf

bind = $mod, x, killactive,

bindm = $mod, mouse:272, movewindow
bindm = $mod, mouse:273, resizewindow

bind = , XF86LaunchA, exec, hyprctl --batch "dispatch togglefloating; dispatch resizeactive exact 1400 800; dispatch centerwindow"

bind = , XF86Search, exec, fuzzel

bind = , XF86AudioMute, exec, pavucontrol

bind = , XF86Eject, exec, grim ~/Downloads/screenshot.png
bind = , XF86Launch7, exec, gtklock
bind = , XF86Launch8, exec, systemctl suspend
bind = , XF86Launch9, exec, shutdown now

bind = Control_L, Left, workspace, -1
bind = Control_L, Right, workspace, +1

bind = Control_L, Down, cyclenext

general {
    layout = "dwindle"

    gaps_in = 0
    gaps_out = 0
    border_size = 0
}

animations {
    enabled = 0

    animation = workspaces, 1, 2, default
}

misc {
    disable_hyprland_logo = true
    force_default_wallpaper = 0
    background_color = 0x222222
}

dwindle {
    force_split = 2
}

windowrulev2 = noanim, class:.*

workspace = 1, persistent:true
workspace = 2, persistent:true
workspace = 3, persistent:true
workspace = 4, persistent:true
workspace = 5, persistent:true
# workspace = 6, persistent:true
# workspace = 7, persistent:true
# workspace = 8, persistent:true
# workspace = 9, persistent:true
